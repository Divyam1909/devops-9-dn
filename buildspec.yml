version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies (none)
  build:
    commands:
      - echo Build started
  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'          # Include everything
  discard-paths: yes   # ✅ This flattens folder structure so files go to root of S3
